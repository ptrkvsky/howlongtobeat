export type Config = typeof config;

export const config = {
  maxWidth: {
    desktop: `1280px`,
    mobile: `95%`,
  },
  zIndex: {
    burgerMenu: 20,
    buttonOpenMenu: 10,
    menuSecondaryBackground: -1,
  },
};
